# Properties file with JDBC and JPA settings.
jpa.showSql=false

##SQL_SERVER Settings for application 
jdbc.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
jdbc.url=jdbc:sqlserver://${ip-Adress}:${sql-server-port}
jdbc.databaseName=${web-databaseName}
jdbc.username=${web-database-user-name}
jdbc.password=${web-database-password}


# Property that determines which Hibernate dialect to use
# (only applied with "applicationContext-hibernate.xml")
hibernate.dialect=org.hibernate.dialect.SQLServerDialect
hibernate.hbm2ddl.auto=none
hibernate.show_sql=false

# Property that determines which database to use with an AbstractJpaVendorAdapter 
jpa.database=SQL_SERVER


#SQL_SERVER Settings for devinfo
jdbc.devinfo.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
jdbc.devinfo.url=jdbc:sqlserver://${ip-Adress}:${sql-server-port}
jdbc.devinfo.databaseName=${devinfo-databaseName}
jdbc.devinfo.username=${devinfo-database-user-name}
jdbc.devinfo.password=${devinfo-database-password}



#Output Path
output.filepath=C://

